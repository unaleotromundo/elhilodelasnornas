/** Estilo «El polvo recuerda»: la ruta raíz no tiene chrome web; sólo contiene el marco de juego. */
import GameCanvas from "@/components/GameCanvas";

export default function Home() {
  return <GameCanvas />;
}
